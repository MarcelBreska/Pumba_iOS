// Base build configuration for the Pumba app target (Release).
// Pulls in CocoaPods settings, then the gitignored Secrets.xcconfig.
#include "../Pods/Target Support Files/Pods-Pumba/Pods-Pumba.release.xcconfig"
#include? "../Secrets.xcconfig"
